<%= form_for(@app) do |f| %>
  <% if @app.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@app.errors.count, "error") %> prohibited this app from being saved:</h2>

      <ul>
      <% @app.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  

<div class="field">
  Creator: <br />
  <%= f.fields_for :creator do |c| %>
    <%= c.label :name %>
    <%= c.text_field :name %>
  <% end %>
  </div>

  <div class="field">
  Models: <br />
  <%= f.fields_for :models do |m| %>
    <%= m.label :name %>
    <%= m.text_field :name %>
  <% end %> 

  </div>


  <div class="field">
    Controller: <br />
    <%= f.fields_for :controllers do |co| %>
      <%= co.label :name %>
      <%= co.text_field :name %>
    <% end %> 
  </div>

 <div class="actions">
    <%= f.submit %>
  </div>

  <% end %>
